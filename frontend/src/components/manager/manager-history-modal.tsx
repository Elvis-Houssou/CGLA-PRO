interface ManagerHistoryModalProps {
  manager: Manager | null
  open: boolean
  onOpenChange: (open: boolean) => void
}

export default function ManagerHistoryModal({ manager, open, onOpenChange }: ManagerHistoryModalProps) {
    return (
        <div></div>
    )
}